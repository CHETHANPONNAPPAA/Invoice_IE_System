spacy>=3.7,<3.8
# spaCy's English model, installed directly as a wheel URL so it's pulled in
# by `pip install -r requirements.txt` alone -- this matters for hosts like
# Streamlit Community Cloud, which only run this file and do NOT run a
# separate `python -m spacy download ...` step.
https://github.com/explosion/spacy-models/releases/download/en_core_web_sm-3.7.1/en_core_web_sm-3.7.1-py3-none-any.whl
pandas>=2.0
networkx>=3.0
python-dateutil>=2.8
requests>=2.31
streamlit>=1.33
plotly>=5.20
matplotlib>=3.8
pyvis>=0.3
nbformat>=5.9
jupyter>=1.0